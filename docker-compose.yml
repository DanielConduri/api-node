version: "3.9"

services:
  api:
    build: ./
    expose:
      - "8000"
    restart: always
    env_file: ./.env
    networks:
      - backend

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d/
    depends_on:
      - api
    restart: always
    networks:
      - backend

networks:
  backend:
    #driver: bridge
